// Frontend entry for the app — import CSS and Flowbite
import '../css/app.css';
// Import Flowbite's JS so interactive components (dropdowns, modals, etc.) work
import 'flowbite';

// You can add other imports here, e.g. bootstrap, Alpine, etc.
